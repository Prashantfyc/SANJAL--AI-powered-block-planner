services:
  - type: web
    name: sanjal-block-planner
    env: python
    plan: free
    region: oregon
    buildCommand: "pip install -r requirements.txt && python backend/seed.py"
    startCommand: "python start.py"
    healthCheckPath: /api/health
    autoDeploy: true
